architectury {
    common rootProject.enabled_platforms.split(',')
}

loom {
    accessWidenerPath = project(":common").file("src/main/resources/plaguesretouchbrush.accesswidener")
}

dependencies {
    implementation "net.fabricmc:fabric-loader:$rootProject.fabric_loader_version"

    //modImplementation "me.shedaniel:architectury:$rootProject.architectury_api_version"

    implementation("me.shedaniel.cloth:cloth-config-fabric:${rootProject.common_cloth_config_version}")
}

configurations{
    shadowCommon
}

jar {
    archiveClassifier = "raw"
}

java {
    withSourcesJar()
}

shadowJar {
    dependsOn(jar)
    from(zipTree(jar.archiveFile))

    configurations = [project.configurations.shadowCommon]
    archiveClassifier = null
}